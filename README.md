# positions-10dma-under-powellrick-dotcom

Auto-updated trading 10DMA dashboard. It lists tickers and whether each is above or below its 10-day moving average. No account sizes.

The self-contained dashboard is `index.html` at the repo root, for GitHub Pages from the `main` branch (`/`).
